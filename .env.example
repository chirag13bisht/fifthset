# Copy this file to ".env" (same folder) and fill in your values.
# On a hosting platform (Railway / Render / etc.) set these as
# environment variables in the dashboard instead of using a file.

# Required by the app. Any non-empty value works for a self-hosted deployment.
APP_ID=fifth-set-collective
APP_SECRET=change-me-to-a-long-random-string

# MySQL connection string provided by your database host.
# Format: mysql://USERNAME:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=mysql://root:"I=Cv+81I5X%VKnO@34.71.64.171:3306/fifthset

# Optional — defaults to 3000. Most platforms inject their own PORT.
# PORT=3000
